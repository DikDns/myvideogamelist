export const metadata = {
  title: "Search",
  description: "Search favorite games here on MVGL!",
};

export default function GamesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
